package com.freelance.speakflow.ui.theme

import androidx.compose.ui.graphics.Color

// Brand colors
val PurplePrimary = Color(0xFF6C63FF)
val PinkAccent = Color(0xFFFF6584)

// Text colors
val TextDark = Color(0xFF2D3436)
val TextGray = Color(0xFFD8DCE1)

// App background (UPDATED as requested)
val BackgroundWhite = Color(0xFFFCFAF7)

// Inputs
val InputGray = Color(0xFFF1F2F6)

// Dark input field (used in Login UI)
val InputDark = Color(0xFF0F172A)

// Accent (Forgot password)
val CyanAccent = Color(0xFF00E5FF)
